services:
  # ---------------------------------------------------------------------------
  # MinIO — S3-compatible object storage
  # Stores: detector model, batch audit logs, theft reports
  # ---------------------------------------------------------------------------
  minio:
    image: minio/minio:latest
    container_name: modelguard-minio
    restart: unless-stopped
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
    command: server /data --console-address ":9001"
    ports:
      - "9000:9000"   # S3 API
      - "9001:9001"   # MinIO Console UI
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ---------------------------------------------------------------------------
  # MinIO Init — one-shot bucket creation using MinIO Client (mc)
  # ---------------------------------------------------------------------------
  minio-init:
    image: minio/mc:latest
    container_name: modelguard-minio-init
    depends_on:
      minio:
        condition: service_healthy
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
    entrypoint: >
      /bin/sh -c "
        mc alias set local http://minio:9000 $$MINIO_ROOT_USER $$MINIO_ROOT_PASSWORD &&
        mc mb --ignore-existing local/modelguard-detectors &&
        mc mb --ignore-existing local/modelguard-auditlog &&
        mc mb --ignore-existing local/modelguard-reports &&
        echo 'Buckets ready.'
      "

  # ---------------------------------------------------------------------------
  # Backend — FastAPI batch theft detection engine (Isolation Forest)
  # ---------------------------------------------------------------------------
  backend:
    build:
      context: ./api
      dockerfile: Dockerfile
    container_name: modelguard-backend
    restart: unless-stopped
    depends_on:
      minio:
        condition: service_healthy
    environment:
      MINIO_ENDPOINT: minio:9000
      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY}
      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY}
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
      ANALYST1_PASSWORD: ${ANALYST1_PASSWORD}
      PARTNER1_PASSWORD: ${PARTNER1_PASSWORD}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD}
    ports:
      - "8000:8000"
    volumes:
      - ./api:/app   # hot-reload in dev

  # ---------------------------------------------------------------------------
  # Frontend — SwaggerAI (nginx serving Swagger UI)
  # Provides an OpenAPI-driven interactive UI for model queries and registration
  # ---------------------------------------------------------------------------
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: modelguard-frontend
    restart: unless-stopped
    depends_on:
      - backend
    ports:
      - "3000:80"

  # ---------------------------------------------------------------------------
  # OE Dashboard — Operations/Engineering monitoring (Streamlit)
  # Internal tool for health checks, audit logs, and theft report review
  # ---------------------------------------------------------------------------
  oe-dashboard:
    build:
      context: ./oe-dashboard
      dockerfile: Dockerfile
    container_name: modelguard-oe-dashboard
    restart: unless-stopped
    depends_on:
      - backend
    environment:
      OE_ADMIN_USER: ${OE_ADMIN_USER}
      OE_ADMIN_PASSWORD: ${OE_ADMIN_PASSWORD}
    ports:
      - "8501:8501"
    volumes:
      - ./oe-dashboard:/app   # hot-reload in dev

volumes:
  minio_data:
    driver: local
